import { css } from '@emotion/css'

export const mainContainer = css`
    display: 'flex';
    align-items: 'center';

`
export const actionDiv = css`
    text-align: 'center';

`

/*
export default makeStyles((theme) => ({
  mainContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  smMargin: {
    margin: theme.spacing(1),
  },
  actionDiv: {
    textAlign: 'center',
  },
}));
*/